
import { WingScreen } from './WingScreen';

import './index.scss';

export const Records = () => {
    return (
        <section className="records-page">
            <WingScreen />
        </section>
    );
}